"""
指令调用限制配置文件
"""


GROUP_ALL = {  # 所有指令的群调用限制
    "command_name": {  # 要限制的指令(不能为别名)
        "all": 24*60*60,  # 对群内所有用户统一的调用限制(即整个群24h内只能调用一次)
        "single": 12*60*60,  # 对群内单个用户的调用限制(即一个用户12h能只能调用一次)
        123456: 48*60*60  # 某个QQ号的用户的调用限制,该限制会覆盖single的限制
    }
}
GROUP_1234567 = {  # GROUP_群号，某个群的调用限制，该限制会覆盖掉GROUP_ALL中的所有内容(对于这个群而言)

}

PRIVATE_ALL = {
    "command_name": 24*60*60  # 指令->调用延时
}
PRIVATE_123456 = { # 对用户123456的限制
    "command_name": 24*60*60  # 指令->调用延时
}
